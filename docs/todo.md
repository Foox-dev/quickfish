# Todo list for sourcefish
Also has some feature ideas and stuff, yada yada.

## TODO
- add formated preview via bat (optional)
- add image preview via some in terminal image renderer (find it out later)
- allow the ability to open files via the file manager (file buffer and shell buffer via 'open')
- selection mode
- move mode (cut/paste maybe?)
- clipboard support (+ register)
- trash and delete support (d and D)
- put expensive tasks on a differant thread
- finish the help page (F1)
- add tests

## ideas
- bulk rename
- jumplist/bookmarks
- git integration
- exiting the program leaves you in the file directory the last where in (might be redundant since we have the Shell Buffer)
- config file

